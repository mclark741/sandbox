function changeText() {

    const header = document.querySelector('h1');
    header.innerText = "Clicked the button"
    console.log("changed text");
}

const user = {
    name: 'matthew',
    username: 'mclark'
};

const users = ['matt', 'nick', 'holly'];
let numbers = [1, 43, 54, 132];
numbers = 3;


const original = 'matt';
const clone = 'MATT';

console.log(user);
